#!/usr/bin/env bash
# Runs the same lint check CI does before each commit. Enable once with:
#   git config core.hooksPath scripts/hooks
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
swiftformat --lint .
swiftlint --strict
